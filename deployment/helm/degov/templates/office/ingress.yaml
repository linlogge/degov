{{- if and .Values.office.enabled .Values.office.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "degov.fullname" . }}-office
  labels:
    {{- include "degov.componentLabels" (dict "component" "office" "Values" .Values "Chart" .Chart "Release" .Release) | nindent 4 }}
  {{- with .Values.office.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.office.ingress.className }}
  ingressClassName: {{ .Values.office.ingress.className }}
  {{- end }}
  {{- if .Values.office.ingress.tls }}
  tls:
    {{- range .Values.office.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.office.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "degov.fullname" $ }}-office
                port:
                  number: {{ $.Values.office.service.port }}
          {{- end }}
    {{- end }}
{{- end }}

